#include <stdio.h>
void check(int num);
void main(){
    int n;
    printf("enter a number: ");
    scanf("%d", &n)
    check(number);
}

void check(int num) {
    if (num%2==0){
        printf("%d is even\n", num);
    }
    else{
        printf("%d is odd\n", num);
    }
}
